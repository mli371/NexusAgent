# 语义上下文缓存实施计划

设计已获批准，以下步骤已完成。详细验证结果见 [中文 review](../../review/semantic-context-cache.md)。

1. 已完成后端：共享作用域描述、可确认的缓存写入、纯规则与余弦计算、有界 Redis 来源、预计算问题向量入口、真实问答编排。
2. 已完成测试：规则正反例、来源过期与容量、权限/版本隔离、精确/语义/miss 路径、一次向量调用、防转存和 Redis 故障。
3. 已完成前端：语义缓存阶段、命中类型、相似度、历史分数来源、未执行状态、组件及浏览器测试。
4. 已完成验证：完整 Maven/worker/前端/浏览器测试、构建、文档与敏感数据检查。使用模拟模型，不读取 .env、不新增付费调用，不自动推送。
