# 真实 RAG 问答第二步实施计划

用户已批准第二步。范围仅为后端真实问答接线，前端留到第三步。

- [x] 显式配置 local/openai 回答模式，保留旧确定性 Agent 示例。
- [x] 所选文档权限、模型覆盖率和输出前权限复查。
- [x] REST/SSE 共用真实执行管线，阶段事件从实际服务边界产生。
- [x] 引用校验、无证据拒答、安全错误、超时和取消。
- [x] 真实模式绕过上下文缓存；状态和工具输出仅保存有界摘要。
- [x] 单元、路由、真实数据库集成回归：272 项后端、26 项 worker 通过，零跳过。
- [x] 中文 review/学习笔记和接口文档。

不修改 `.env`，不自动重建现有向量，不调用付费模型，不重启用户进程，不提交 Git。
不改变 Pi 文档管理审批流程。只验证模型输出的引用结构，不声称验证语义蕴含。
