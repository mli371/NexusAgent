# 学习工作台实现计划

依据：已批准的 `2026-09-11-learning-workbench-design.md`。

1. [完成] 新增 owner-scoped 工具摘要与详情投影，run GET 补充本次问题与文档 ID；专项 25 项、完整后端 194 项测试通过。
2. [完成] 创建独立 React / TypeScript 前端，接文档、身份、任务提交与安全恢复查看；构建通过。
3. [完成] 接持久事件 SSE、真实流程节点、人工决定、取消与调用详情；19 项状态、解析与组件测试通过。
4. [完成] 4 项合成浏览器测试及桌面/手机截图检查通过；1 项隔离真实全链路浏览器测试通过，验证上传、两次审批、持久化结果和刷新恢复；worker 26 项测试通过。运行指南、学习笔记、中文 review 和相关索引已更新。

不读取模型密钥，不执行付费模型测试，不自动提交 Git，不改变现有审批策略。正式界面仅展示真实后端状态；测试数据只用于测试。
