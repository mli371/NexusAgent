package com.nexusagent.enterprise.audit;

public enum AuditEventType {
    DOCUMENT_UPLOADED,
    DOCUMENT_CHUNKED,
    DOCUMENT_EMBEDDED,
    QUERY_EXECUTED,
    AGENT_QUERY_EXECUTED,
    FORCE_RECHUNKED
}
